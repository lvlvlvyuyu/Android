<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="exercise.android.com.accesswords.MainActivity"
    tools:showIn="@layout/activity_main">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="全部"
        android:id="@+id/buttonAll"
        android:layout_marginTop="95dp"
        android:layout_marginStart="71dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="增加"
        android:id="@+id/buttonInsert"
        android:layout_alignTop="@+id/buttonAll"
        android:layout_toEndOf="@+id/buttonAll"
        android:layout_marginStart="47dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="删除"
        android:id="@+id/buttonDelete"
        android:layout_below="@+id/buttonAll"
        android:layout_alignStart="@+id/buttonAll" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="全部删除"
        android:id="@+id/buttonDeleteAll"
        android:layout_below="@+id/buttonInsert"
        android:layout_alignStart="@+id/buttonInsert" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="更新"
        android:id="@+id/buttonUpdate"
        android:layout_below="@+id/buttonDelete"
        android:layout_alignStart="@+id/buttonDelete" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="查询"
        android:id="@+id/buttonQuery"
        android:layout_below="@+id/buttonDelete"
        android:layout_alignEnd="@+id/buttonDeleteAll" />
</RelativeLayout>
